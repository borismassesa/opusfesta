import { ComingSoon } from '@/components/vendor/ComingSoon';

export default function BookingsScreen() {
  return <ComingSoon icon="briefcase-outline" title="Bookings" body="Track confirmed bookings from quote to completed." />;
}
